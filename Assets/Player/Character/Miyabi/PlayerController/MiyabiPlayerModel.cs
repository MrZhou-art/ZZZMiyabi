using UnityEngine;

namespace Player.Character.Miyabi.PlayerController
{
    public class MiyabiPlayerModel : MonoBehaviour
    {
        void Start()
        {
            // 初始化模型缩放为1（x, y, z轴均为1）
            transform.localScale = Vector3.one;
        }

        void Update()
        {
        
        }
    }
}
